"""Empty on purpose: its presence at the repo root makes pytest add this
directory to sys.path, so tests/ can `import costs`, `import capture`, etc.
without a src-layout package or editable install."""
